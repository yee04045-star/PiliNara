import UIKit

// Status-bar taps are delivered to Flutter through WidgetsBindingObserver.
// This target file intentionally contains no custom UIWindow or touch
// interception, so it cannot cover Flutter's first frame or steal touches.
